import React from 'react';
import PageGap from './PageGap';

export default {
  component: PageGap,
  title: 'Components/Pagination/PageGap',
};

const Template = (args) => <PageGap {...args} />;

export const Default = Template.bind({});
Default.args = {};
